Этот каталог монтируется в `/docker-entrypoint-initdb.d` в контейнер с Постгресом.
Скрипты имиджа `postgres` выполняют `*.sql` один раз при первом запуске (когда баз нет).

Так создаётся БД-юзер приложения, база для него и выдаются права на базу.
